// #define macro_name character_sequence
#include<stdio.h>

#define LIMIT 10
#define TOP (LIMIT + 1)
#define NAME "Ashok Kumar"
int main(){
    int counter;
    printf(NAME);
    for(counter = 1; counter<= TOP; counter++){
        printf("%d\n",counter);
    }
    return 0;
    
}
